[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atividade-pratica-c14"
version = "0.1.0"
description = "Gerenciador de biblioteca em Python"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["app"]